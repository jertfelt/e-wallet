import { useState } from "react";

const Card = ({
  cardbrand, cardtype, bankname, banklogo, brandimg, cardnumber, cardholderName, expmonth, expyear
}) => {
  return ( 
    <div>
      
    </div>
   );
}
 
export default Card;